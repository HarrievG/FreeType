# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: 'windows-2019'

variables:
  BuildConfiguration: 'Release'

steps:
#- task: CMake@1
#  inputs:
#    cmakeArgs: ..
#- task: CMake@1
#  inputs:
#    cmakeArgs: '--build . --target FreeType --config release'
#- task: CMake@1
#  inputs:
#    cmakeArgs: '--build . --target FreeType --config debug'

- script: |
    @echo on    
    cmake $(Build.SourcesDirectory)
    cd $(Build.ArtifactStagingDirectory)
    cmake --build $(Build.SourcesDirectory) --target FreeType --config release
    cmake --build $(Build.SourcesDirectory) --target FreeType --config debug
    dir
  displayName: 'Cmake Build'
- task: PublishBuildArtifacts@1
  inputs:
    PathtoPublish: '$(Build.ArtifactStagingDirectory)'
    ArtifactName: 'drop'
    publishLocation: 'Container'